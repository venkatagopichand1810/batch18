// reverses the order of the elements in the array..it will changes the original array and it wil not return new array...

// let numbers = [1, 2, 3, 4, 5, 6];
// numbers.reverse();
// console.log(numbers)


let userComments = [
    {user: 'venkat', text: "movie is nice"},
    {user: 'ram', text: "movie is avg, not having much intrest"}
]

userComments.reverse();
console.log(userComments)