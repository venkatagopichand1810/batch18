
// method in javascript is used to find the index of the first occurence of the spdived the value,, -1

//array.indexOf(element, index)

const mobiles = ["lg", 'oppo', 'vivo'];

const index = mobiles.indexOf("oppofd");

console.log(index)