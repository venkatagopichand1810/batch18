//foreach method in javascript is used to execute the given function once for each element in the array..


// array.forEach(value)

const numbers = [1, 2, 3, 4, 5];
numbers.forEach(function(number) {
    console.log(number)
})